cluster:
  replicas: 2
  nodes: 6

persistence:
  enabled: true
  size: 20Gi

tls:
  enabled: true
  autoGenerated: true

auth:
  enabled: true
  password: redis@12345

service:
  type: ClusterIP

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi
