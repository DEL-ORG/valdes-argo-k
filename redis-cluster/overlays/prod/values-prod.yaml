fullnameOverride: redis
nameOverride: redis

architecture: replication

image:
  repository: redis
  tag: 7.4.1



auth:
  enabled: true
  password: redis@12345

master:
  persistence:
    enabled: true
    size: 2Gi

replica:
  replicaCount: 3
  persistence:
    enabled: true
    size: 2Gi

tls:
  enabled: true
  autoGenerated: true

service:
  type: ClusterIP
