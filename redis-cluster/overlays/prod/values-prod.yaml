architecture: replication

auth:
  enabled: true
  password: redis@12345

master:
  persistence:
    enabled: true
    size: 20Gi

replica:
  replicaCount: 2
  persistence:
    enabled: true
    size: 20Gi

tls:
  enabled: true
  autoGenerated: true

service:
  type: ClusterIP
