apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: redis-cluster

resources:
  - namespace.yaml
  - secret.yaml

helmCharts:
  - name: redis
    repo: https://charts.bitnami.com/bitnami
    version: 19.4.0   # check for latest version
    releaseName: redis-cluster
    namespace: redis-cluster
    valuesFile: values.yaml
