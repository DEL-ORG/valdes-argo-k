apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

patches:
  - target:
      kind: ConfigMap
      name: lb-cleaner-config
    patch: |-
      - op: replace
        path: /data/approved-namespaces
        value: "argocd,test-monitoring"
