apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets-operator-staging
spec:
  source:
    path: platform-tools/external-secrets/base
    helm:
      valueFiles:
        - base-values.yml
        - overlays/staging/values-patch.yml
  destination:
    namespace: external-secrets2
