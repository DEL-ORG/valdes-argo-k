{{- if .Values.externalSecret.enabled }}
# Placeholder for external secret resource (e.g., ExternalSecrets or SealedSecrets)
# Example: create a Kubernetes Secret here if you prefer
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.externalSecret.name }}
stringData:
  # Fill via sealed secret / external secret tooling in your pipeline
  DUMMY: "REPLACE_ME"
type: Opaque
{{- end }}
