{{- if .Values.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  # Put placeholder keys here or leave empty and expect user to create secret separately
  SOME_API_KEY: ""
{{- end }}
